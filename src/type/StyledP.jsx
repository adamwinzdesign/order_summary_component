import styled from "styled-components";
import { desaturated_blue } from "../colors";

const StyledP = styled.p`
	font-size: 16px;
	font-weight: 500;
	color: ${desaturated_blue};
	line-height: 1.75rem;
`;

export default StyledP;
